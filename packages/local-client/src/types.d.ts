/**
 * If packages dont have any types file but packages works for us we declare a module so typescript wont show us error which says there is no type files for the packages.
 */

declare module "monaco-jsx-highlighter";
